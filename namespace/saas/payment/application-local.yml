server:
  port: 9005

brokers: localhost:29092
spring:
  application:
    name: payment
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration
  kafka:
    admin:
      fail-fast: false
  profiles:
    active: dev
  security:
    jwt:
      secret: my-super-secret-key-my-super-secret-key
    oauth2:
      resourceserver:
        jwt:
          secret: my-super-secret-key-my-super-secret-key
          jws-algorithm: HS256# ✅ same secret in all services
  data:
    redis:
      host: localhost
      port: 6379
      password: MyStrongPassword
  datasource:
    url: jdbc:mysql://localhost:3309/mydatabase
    username: user
    password: mypass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: smtp.gmail.com
    username: atulrathodc@gmail.com
    password: "fvpm xuzo kdyu wldi"
    properties:
      mail:
        smtp:
          auth: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
  freemarker:
    template-loader-path: classpath:/templates/
    suffix: .ftl
    cache: false
    charset: UTF-8
    expose-spring-macro-helpers: true
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
  cloud:
    vault:
      uri: http://127.0.0.1:8200
      kv:
        enabled: false
        backend: secret
        default-context: example

logging:
  level:
    org:
      springframework:
        security: INFO
    org.apache.kafka.clients.NetworkClient: ERROR
    org.apache.kafka.clients.producer.ProducerConfig: ERROR
    org.apache.kafka.clients.consumer.ConsumerConfig: ERROR
    org.apache.kafka.clients: ERROR

support:
  email: atulrathodc@gmail.com
  name: Atul Rathod

stripe:
  secret:
    key: sk_test_XXXXXXXXXXXXXXXXXXXX

openai:
  model: gpt-4o-mini
  api:
    key: sk-svcacct-IV3H3npvGdyco2RPOQD2jLZx4vtPSDenb-nSoHeBCCrzSyHA1pS3Loi-gKowfOT3BlbkFJHIW8ui5Y43Xz_iU3gO7vI5XnSH4PMrlO-8uX9DIUDtUC2LpRTO532KeVyitQMA
    url: https://api.openai.com/v1/chat/completions

my:
  framework:
    enabled: true
    apiKey: abc-123-secret
management:
  tracing:
    enabled: true
    sampling:
      probability: 1.0

  otlp:
    tracing:
      enabled: true
      endpoint: http://host.docker.internal:9095
      protocol: grpc
      export:
        enabled: true
    metrics:
      export:
        enabled: false  # disable OTLP metrics export
        step: 10s
#  zipkin:
#    enabled: true
#    base-url: http://localhost:3200/api/traces
#    sender: web
#  otlp:
#    tracing:
#      endpoint: http://localhost:4317   # sends to Tempo
#    endpoint: http://localhost:4317
#    protocol: grpc
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"
#        include: health,info,refresh,prometheus
resilience4j:
  circuitbreaker:
    instances:
      myService:
        registerHealthIndicator: true
        slidingWindowSize: 10
        failureRateThreshold: 50
        waitDurationInOpenState: 50s
        permittedNumberOfCallsInHalfOpenState: 3
  retry:
    instances:
      myService:
        maxAttempts: 3
        waitDuration: 2s
custom:
  property1: value updated

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${instanceId:${random.value}}

